import React from "react";
import s from "./page.module.scss";
const Success = () => {
    return (
        <main className={s.main}>
            <p>Successfully emailed Japautos</p>
        </main>
    );
};

export default Success;
